import { Box } from "@mui/material";

export const DrawerData: React.FC = () => {

    return (
        <Box sx={{
            display: 'flex',
            flexDirection: 'column',
            width: '66%',
        }}>
        </Box>
    );
};
